{
  "framework": "vite",
  "installCommand": "pnpm install --no-frozen-lockfile || npm ci || npm install",
  "buildCommand": "pnpm run build || npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "echo",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

