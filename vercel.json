{
  "installCommand": "npm ci || npm install",
  "buildCommand": "npm run build",
  "framework": null,
  "ignoreCommand": "echo", 
   "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

